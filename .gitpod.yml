ports:
  - port: 3000
    onOpen: open-preview
tasks:
  - before: gem update --system && gem install bundler && bundler update --bundler
    init: >
      bundle install --without production &&
      rails db:migrate
    command: >
      rails server
github:
    prebuilds:
        pullRequestsFromForks: true
